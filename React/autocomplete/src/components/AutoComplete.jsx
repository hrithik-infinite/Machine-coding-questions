
const AutoComplete = ({
    placeholder,
    fetchSuggestion ,
    dataKey,
    customLoader,
    onSelect = () =>{}
    customStyles = {{}}
    // staticData={data}
}) => {
  return (
    <div>AutoComplete</div>
  )
}

export default AutoComplete